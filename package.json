{
  "name": "dns-bot",
  "version": "1.0.0",
  "description": "DNS monitoring service using Cloudflare Workers",
  "main": "dist/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "dns",
    "cloudflare",
    "workers",
    "monitoring"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.28.1"
  },
  "dependencies": {
    "node-telegram-bot-api": "^0.66.0"
  }
}
